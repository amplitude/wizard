# TUI Redesign Mocks

Proposal for a refreshed wizard TUI that adopts Claude Code's calm, prompt-centric look while keeping Amplitude brand alignment and modern terminal-app conventions.

> The "current" frames in this doc are real `lastFrame()` captures from `ink-testing-library`, generated by [`src/ui/tui/__tests__/capture-current-screens.test.tsx`](../src/ui/tui/__tests__/capture-current-screens.test.tsx). The full unedited capture lives at [`docs/_tui-current-state.md`](./_tui-current-state.md).
>
> Re-generate captures with:
>
> ```bash
> pnpm exec vitest run src/ui/tui/__tests__/capture-current-screens.test.tsx
> ```

---

## 1. Goals

| Goal | What changes |
|---|---|
| **Match Claude Code's look** | Left-aligned content, single accent color, sparse chrome, prompt at the bottom, brand pinned to the top-left only. |
| **Industry best practices** | No centered text, no decorative emoji, consistent picker chevron, single hint bar, bold heading + dim subtitle pattern, density over padding. |
| **Keep Amplitude brand** | Lilac/blue accent retained for active state; brand glyph (`▎`) prefixes the top bar; existing `Brand` palette in `styles.ts` reused unchanged. |

## 2. Design principles

1. **One focal point per screen.** Heading, body, action — in that order. Decorative panels (Discovered facts, marketing taglines) move to a sidebar or get demoted to a single line.
2. **Left-aligned everything.** Centered ASCII text reads as "wizard from 2008". Modern TUIs (Claude Code, `gh dash`, `lazygit`, `bun create`) align flush left with a 2-space gutter.
3. **Persistent thin chrome.** A single 1-line top bar (brand + breadcrumb + journey) and a single 1-line bottom hint bar. No banners, no double separators.
4. **One accent color in motion.** Brand lilac for active picker rows and in-progress task bullets; brand blue for links; everything else is gray-scale text. The current palette already supports this — just use it more consistently.
5. **No emoji in load-bearing UI.** `🎉` and `💬` move out of headings; replaced by `✓`/`◆` from `Icons`. Emoji read as cute, not professional, and break alignment in monospace.
6. **Picker chevron, not bracketed numbers.** `❯ Sign in` instead of `▸ [1] Continue — sign in to Amplitude (existing account)`. Numbers move to a secondary-color hint at the right margin (`1` dim).
7. **Density.** Pre-run summary, target line, and tagline collapse into the top bar. Trim trailing blank lines that pad screens to 6+ rows of empty space.

## 3. Persistent chrome

### 3a. Top bar

**Current** (3 lines: stepper + "Amplitude Wizard" header + separator)

```
○ Welcome ─ ● Auth ─ ○ Setup ─ ○ Verify ─ ○ Done
Amplitude Wizard                                              · Acme Corp / Production / US
─────────────────────────────────────────────────────────────────────────────────────────────────
```

**Proposed** (1 line: brand + journey + context, all in one row, separator only when content needs framing)

```
▎ amplitude wizard   ● Welcome  ○ Auth  ○ Setup  ○ Verify  ○ Done   ·   Acme Corp / Production / US
```

The `▎` glyph (U+258E) is a thin lilac-blue gradient stripe — the brand mark in 1 character. The lowercased word-mark matches Claude Code's typographic register. Journey stepper and breadcrumb share one row; if the terminal is too narrow (<100 cols), the breadcrumb wraps to a second row and the journey becomes dot-only (`● ○ ○ ○ ○`).

### 3b. Bottom bar

**Current** (1 line, varies per-screen, brackets vary)

```
[/] Commands [Tab] Ask a question [Esc] Cancel  [Enter] Continue  [r] Retry browser
```

**Proposed** (2 lines: prompt + a single muted hint row)

```
❯ ▎
  enter accept · esc back · / commands · tab ask · ctrl-c quit
```

The `❯` is the live console prompt (already used in `ConsoleView`). Hints are lowercased and `·`-separated — same convention as Claude Code and most modern CLIs. Per-screen hints (`r retry`, `m manual`) join the same row, screen-specific keys first, defaults last.

---

## 4. Per-screen mocks

For each screen: real captured "current" frame on the left, proposed mock on the right.

### 4.1 IntroScreen

#### Detecting

Current

```
                                          Amplitude Wizard
                                AI-powered analytics setup in minutes

                                     Target     /projects/my-app

                                    ⠋ Scanning /projects/my-app…
```

Proposed

```
  ⠋ Scanning /projects/my-app…
    Looking for a framework, package manager, and existing Amplitude setup.

    esc cancel
```

The big banner is gone — the top bar already says "amplitude wizard". The target path is the spinner's subject, not a separate row. Subtitle explains *what* we're scanning for, removing the user's "is it stuck?" anxiety.

#### Detected (Next.js)

Current

```
                                          Amplitude Wizard
                                AI-powered analytics setup in minutes

                                   Target     /projects/my-app
                                   Framework  ▲ Next.js (detected)

              ⚠ No project manifest found here. This may not be the project you meant.

                    Sign in to an existing Amplitude account, or create a new one
                   ▸ [1] Continue — sign in to Amplitude (existing account)
                     [2] Continue — create a new account (new organization)
                     [3] Change framework
                     [4] Change directory
                     [5] Cancel
```

Proposed

```
  Detected ▲ Next.js in /projects/my-app

  We'll install the Amplitude SDK, plan events with you, and write
  tracking code. Every file change is shown for review before it's
  applied.

  ❯ Sign in to your Amplitude account
    Create a new account
    Change framework
    Change directory
    Cancel

  ⚠  No package.json here — double-check this is the right directory.
```

Heading carries Target + Framework + glyph in one line. The "Sign in to an existing Amplitude account, or create a new one" sub-heading is dropped — the picker options speak for themselves. Bracketed `[1]…[5]` numbers are dropped from the labels; they remain available as keypresses but render only on hover (when a row is highlighted, its number appears right-aligned in muted text).

#### Welcome back (returning user)

Current

```
                                 Welcome back, kelson@amplitude.com
                                         Acme Analytics · US
                              3 events instrumented · last run just now

                               Target     /projects/my-app
                               Framework  ▲ Next.js (detected)

              ⚠ No project manifest found here. This may not be the project you meant.

      Next: install the SDK and propose an event plan you'll review before any code is written.

                You're signed in as kelson@amplitude.com. Continue to workspace setup.
               ▸ [1] Continue — workspace setup (same login)
                 [2] Change framework
                 [3] Change region
                 [4] Change directory
                 [5] Cancel
```

Proposed

```
  Welcome back, kelson@amplitude.com
    Acme Analytics · US · 3 events · last run just now

  ▲ Next.js detected in /projects/my-app

  ❯ Continue to workspace setup
    Change framework
    Change region
    Change directory
    Cancel

  ⚠  No package.json here — double-check this is the right directory.
```

Three condensed rows replace the eight in the current version. The "Next: install the SDK…" coaching line is dropped — returning users have already seen it. Sign-in confirmation moves to the picker label ("Continue to workspace setup" implies "still you").

#### Resume from checkpoint

Current

```
                                          Amplitude Wizard

                                 A previous session was interrupted.
                                  Framework: Next.js
                                  Organization: Acme Corp


                                 ▸ [1] Resume where you left off
                                   [2] Start fresh
                                   [3] Cancel (exit wizard)
```

Proposed

```
  Previous session was interrupted

    Framework      ▲ Next.js
    Organization   Acme Corp

  ❯ Resume where you left off
    Start fresh
    Cancel
```

Two-column key/value table for the saved-state fields aligns with the "Discovered" list later in the run; consistent visual language across screens.

### 4.2 SetupScreen — manual framework picker

Current

```
Detecting project configuration…
```

(After detection, this screen renders a `PickerMenu` per unresolved question.)

Proposed (detection state)

```
  ⠋ Detecting project configuration…
    Reading framework metadata, build tool, package manager.
```

Proposed (picker state, e.g. "Pick your build tool")

```
  Pick your build tool
    Detection couldn't tell — choose what your project uses.

  ❯ Vite
    Webpack
    Turbopack
    Other
```

Body line *says why the user is being asked* (detection failed) — currently this context is missing.

### 4.3 AuthScreen

#### OAuth waiting

Current

```
⠋ Signing you in…

If the browser didn't open, copy and paste this URL:
https://app.amplitude.com/oauth?response_type=code&client_id=wizard

[R] Retry browser  [M] Enter API key manually  [Esc] Cancel
```

Proposed

```
  ⠋ Signing you in
    Opening your browser. If it didn't open, paste this URL:

    https://app.amplitude.com/oauth?response_type=code&client_id=wizard

  r retry browser · m enter api key manually · esc cancel
```

Per-screen hints fold into the bottom hint convention (lowercase, `·` separator). URL gets one row of breathing room above and below — currently it's tight against the heading.

#### Org picker

Current

```
Select your organization


▸ [1] Acme Corp
  [2] Globex
  [3] Initech
```

Proposed

```
  Choose an organization

  ❯ Acme Corp
    Globex
    Initech
```

Same picker convention as IntroScreen. The trailing blank line under the heading goes away — Ink already adds 1 row of margin via `marginBottom={1}`.

#### Project picker

Current

```
✓ Organization: Acme Corp

Select a project
in Acme Corp


▸ [1] Production
  [2] Staging
  [3] Internal Tools
  [4] Create new project…
  [5] Start over
```

Proposed

```
  ✓ Organization · Acme Corp

  Choose a project

  ❯ Production
    Staging
    Internal Tools
    + Create new project…
    ↩ Start over
```

The "completed step" pin is shortened to a single line with `·` instead of `:` to match the breadcrumb in the top bar. Synthetic actions ("Create new", "Start over") get a leading glyph (`+`, `↩`) to visually distinguish them from real choices.

### 4.4 RegionSelectScreen

Current

```
Select your data region
Select the region for your Amplitude project. This should match the region your organization uses.


▸ [1] United States (app.amplitude.com)
  [2] Europe (app.eu.amplitude.com)

· Data residency affects API endpoints and compliance. You can change this later with /region.
```

Proposed

```
  Choose your data region
    Match the region your organization uses.

  ❯ United States    app.amplitude.com
    Europe           app.eu.amplitude.com

    Data residency affects API endpoints and compliance.
    Change later with /region.
```

Two-column picker labels (option name + endpoint) align like the resume-checkpoint table. The dot-bullet footer becomes plain dim text, indented two spaces — bullet glyphs are reserved for real picker rows, not advisories.

### 4.5 SignupEmailScreen

Current

```
Create your Amplitude account
Enter your email to get started

your.email@example.com


· We'll use this to create your Amplitude account
```

Proposed

```
  Create your Amplitude account
    Enter your email to get started.

  ❯ │
    your.email@example.com

    We'll use this to create your Amplitude account.
```

The `❯` prefix marks the active text input. The placeholder appears below the cursor in dim text, only when the input is empty — same pattern as Claude Code's prompt placeholder. `marginTop` on the helper line tightens.

### 4.6 SigningUpScreen

Current

```
Create your Amplitude account
Checking your account…

kelson@amplitude.com

⠋ · contacting Amplitude…
```

Proposed

```
  Create your Amplitude account
    Checking your account…

    kelson@amplitude.com

  ⠋ contacting Amplitude
```

Spinner row drops the leading `·` and the redundant trailing `…` — the spinner glyph already conveys "in progress".

### 4.7 SignupFullNameScreen

Current

```
One more thing
What's your full name?

First Last

```

Proposed

```
  One more thing
    What's your full name?

  ❯ │
    First Last
```

Same pattern as SignupEmailScreen.

### 4.8 ToSScreen

Current

```
Terms of Service
By continuing, you agree to Amplitude's terms

Please review and accept our Terms of Service and Privacy Policy to continue:

→ Terms: https://amplitude.com/terms
→ Privacy: https://amplitude.com/privacy


▸ [1] I accept the Terms of Service and Privacy Policy
  [2] I do not accept

· This is required to create an Amplitude account
```

Proposed

```
  Terms of Service
    Please review before continuing.

    Terms      https://amplitude.com/terms
    Privacy    https://amplitude.com/privacy

  ❯ I accept
    I do not accept

    Required to create an Amplitude account.
```

The double-statement of intent ("By continuing… / Please review…") collapses to one line. The two URLs become a key/value table. Picker labels are stripped to verb form; the "I accept the Terms of Service and Privacy Policy" mouthful was already redundant given the heading.

### 4.9 CreateProjectScreen

Current

```
Create a new Amplitude project
in Acme Corp

Project name
Letters, numbers, and spaces. You can rename it later in Amplitude.

My new project

· Press Enter to create, Esc to go back.
```

Proposed

```
  Create a new project
    in Acme Corp

  Project name
    Letters, numbers, and spaces. You can rename it later.

  ❯ │
    My new project
```

The `· Press Enter to create, Esc to go back.` footer is redundant with the bottom hint bar (which already shows `enter accept · esc back`). Drop it.

### 4.10 DataSetupScreen

Current

```
Checking project setup

⠋ Analyzing your Amplitude project…

Looking for existing SDK installation and event data
```

Proposed

```
  ⠋ Analyzing your Amplitude project
    Looking for existing SDK installation and event data.
```

Three rows collapse to two — heading is folded into the spinner subject.

### 4.11 ActivationOptionsScreen

Current

```
Your SDK is partially set up ─ waiting for events

We can see your project is configured but hasn't received many events yet.
What would you like to do?


▸ [1] Help me test locally (run the setup agent)
  [2] I'm blocked — write a support report (open log + save shareable report)
  [3] Take me to the docs (https://amplitude.com/docs/sdks)
  [4] I'm done for now (exit and resume later)
```

Proposed

```
  SDK installed — waiting for events
    Your project is configured but hasn't received events yet.

  What would you like to do?

  ❯ Help me test locally                 run the setup agent
    Write a support report               open log · save shareable report
    Open the docs                        amplitude.com/docs/sdks
    Exit and resume later
```

Each picker row gets a primary action (left) and a dim hint (right), aligned at the same column. Reads as a menu, not a paragraph. The "I'm blocked"/"I'm done" first-person framing becomes neutral verb-form to match the rest of the wizard.

### 4.12 RunScreen

This is the highest-stakes screen — users stare at it for the entire agent run.

Current

```
Discovered · 5 facts
 ✓ Framework         JavaScript (Web)
 ✓ TypeScript        yes
 ✓ Package manager   Yarn V1
 ✓ Project           Amplitude
 ✓ Region            US
0 done · 4 to go · 55s (cold start: ~30–60s)

Tasks

› Detecting your project setup
○ Install Amplitude
○ Plan and approve events to track
○ Wire up event tracking

 ◇ Detecting your project setup

  Progress   Logs   Snake (WASD)                                                 ← → to switch tabs
```

Proposed (wide terminal ≥ 110 cols, 2-column layout)

```
  Setting up Amplitude in Next.js                                  0/4 · 55s · cold start

  Tasks                                          Discovered
  ◐ Detecting your project setup                   Framework         JavaScript (Web)
  ○ Install Amplitude                              TypeScript        yes
  ○ Plan and approve events to track               Package manager   Yarn V1
  ○ Wire up event tracking                         Project           Amplitude
                                                   Region            US

  ⠋ Reading package.json

  ── progress ──── logs ─── snake ──                                          ← → switch
```

Proposed (narrow ≤ 110 cols, single column)

```
  Setting up Amplitude in Next.js
    0/4 done · 55s · cold start

  Tasks
  ◐ Detecting your project setup
  ○ Install Amplitude
  ○ Plan and approve events to track
  ○ Wire up event tracking

  Discovered
    Framework         JavaScript (Web)
    TypeScript        yes
    Package manager   Yarn V1
    Project           Amplitude
    Region            US

  ⠋ Reading package.json

  progress · logs · snake                                                     ← → switch
```

Changes

- **Heading carries setup target.** "Setting up Amplitude in Next.js" replaces the missing heading on the current screen. Right-aligned counter `0/4 · 55s · cold start` collapses three current rows.
- **Discovered moves to a side column** on wide terminals — already the post-#688 layout, but the "·  5 facts" header and `✓` row glyphs go away. The fact list reads as a key/value table; the absence of a glyph is fine because the section heading already establishes "these are facts".
- **Task glyphs unify with the journey stepper.** `◐` (in-progress) replaces the `›` arrow; `○` for pending; `●` for done. Same as IntroScreen's stepper, so the user learns one glyph language across the wizard.
- **Status pill is now plain text with a leading spinner.** `◇` glyph dropped — the spinner conveys "live", and a static diamond next to a static label was redundant.
- **Tab bar uses dim dashes as separators**, with the active tab highlighted in lilac. The current tab bar's `Snake (WASD)` reads as a typo; the new one drops parens.

### 4.13 McpScreen

Current

```
💬 Chat with your Amplitude data
We'll wire the Amplitude MCP into Claude Code, Cursor, Claude Desktop, and other AI tools you have
installed. You can then ask questions, build charts, and check metrics from chat (e.g. "show me
yesterday's signups").

⠋ Looking for supported AI tools…
```

Proposed

```
  Chat with your Amplitude data
    Wire Amplitude MCP into your AI tools — Claude Code, Cursor, Claude Desktop —
    so you can ask "show me yesterday's signups" from chat.

  ⠋ Looking for supported AI tools
```

`💬` dropped (industry best practice; emoji break alignment in monospace). Body is one paragraph instead of three, with the example moved inline.

After detection (proposed):

```
  Chat with your Amplitude data
    Found 3 AI tools installed.

  ❯ Install in all 3                Claude Code · Cursor · Claude Desktop
    Pick which to install in
    Skip
```

### 4.14 DataIngestionCheckScreen

Current

```
  Start your app and trigger some user actions

  → Start your dev server, then visit it and click around

  Your SDK is installed. Once you interact with your app, events will start flowing into
  Amplitude.

    → If your dev server or build was already running, restart it so the new env values load.

  ⠋ Listening for events…


  [Enter] or [q] Skip verification  [x] Exit and resume later
```

Proposed

```
  Trigger some events
    Start your dev server, visit it, and click around. We'll watch for events.

  ⠋ Listening for events…

    Heads up: if your dev server was already running, restart it so the new env
    values load.
```

Heading is action-led ("Trigger some events" is what we want the user to do). Three near-identical messages ("Start your app", "Start your dev server", "Once you interact…") collapse to one. Restart reminder demoted to a "heads up" footnote — relevant but not primary. Skip-verification hint moves to the bottom hint bar.

### 4.15 SlackScreen

Current

```
Slack Integration

Connect Amplitude to Slack for chart previews, dashboard sharing,
and real-time tracking plan notifications.

 Connect the "Amplitude" Slack app to your workspace?

    Connect
    Skip for now
```

Proposed

```
  Connect Slack
    Get chart previews, dashboard sharing, and tracking-plan
    notifications in your Slack workspace.

  ❯ Connect
    Skip for now
```

Heading is a verb. The "Connect the 'Amplitude' Slack app to your workspace?" question is folded into the verb-form picker — currently the screen has both, which doubles the read.

### 4.16 LogoutScreen

Current

```
Log Out

 Clear stored Amplitude credentials (wizard session + project binding)?

    Log out
    Cancel
```

Proposed

```
  Log out
    Clear your stored Amplitude credentials (wizard session and
    project binding) from this machine.

  ❯ Log out
    Cancel
```

Question form replaced by a description. The current "?" mid-screen confuses Esc behavior — esc-cancel and "click Cancel" mean the same thing, both should land here.

### 4.17 LoginScreen

Current

```
Re-authenticate

⠋ Refreshing credentials…
```

Proposed

```
  Re-authenticate
    Trying to refresh your stored credentials…

  ⠋ Refreshing
```

Heading is fine; explicit "trying to refresh stored credentials" subtitle gives the user something to read while it spins (current screen has 2-3 seconds of dead text).

### 4.18 OutroScreen

#### Success

Current

```
  🎉 Amplitude is live!

  ● Installed @amplitude/analytics-browser
  ● Added .env.local with AMPLITUDE_API_KEY
  ● Added 3 planned events to your tracking plan


  ▸ [1] Open Amplitude (amplitude.com)
    [2] Exit
```

Proposed

```
  ✓ Amplitude is live
    Setup complete in 2m 14s.

  Changes
    + Installed @amplitude/analytics-browser
    + Added .env.local with AMPLITUDE_API_KEY
    + Added 3 planned events to your tracking plan

  ❯ Open Amplitude                 amplitude.com
    Exit
```

`🎉` dropped — `✓` in success-green is the modern equivalent. Setup duration surfaces here (currently nowhere). Changes use `+` (additive), aligning with diff conventions and reusing the picker hint-column pattern.

#### Error

Current

```
  ✗ Setup failed

  The agent could not detect your framework. Re-run with --menu to pick one manually.

  → Check your API key and network connection
  → Run the wizard again with --debug for more detail
  → Full log: <tmp>/amplitude-wizard.log (press L to open)
  → Press C to write a sanitized bug report
  → Docs: https://amplitude.com/docs/get-started/quickstart

  Press any key to exit
```

Proposed

```
  ✗ Setup failed
    The agent could not detect your framework. Re-run with --menu to pick
    one manually.

  Try these
    + Check your API key and network connection
    + Re-run with --debug for more detail
    + Open the log     <tmp>/amplitude-wizard.log     l
    + Save bug report                                 c
    + Open docs        amplitude.com/docs/get-started/quickstart

  press any key to exit
```

"Try these" replaces the bare `→` list. Each row has a primary action and a right-aligned single-key hint, mirroring the picker convention. The "press any key to exit" is lowercase to read as ambient, not a button label.

#### Cancel

Current

```
  ─ Setup cancelled

  Setup cancelled.

  Resume later
  Pick up where you left off — run npx @amplitude/wizard in this directory anytime.

  Manual setup: https://amplitude.com/docs/get-started/quickstart

  Press any key to exit
```

Proposed

```
  Setup cancelled

  Resume later
    Run npx @amplitude/wizard in this directory anytime.

  Manual setup
    amplitude.com/docs/get-started/quickstart

  press any key to exit
```

The `─ Setup cancelled` heading-glyph is non-semantic (a horizontal rule rendered as a heading is confusing); drop it. The redundant "Setup cancelled." sentence under a heading that already says "Setup cancelled" is dropped.

### 4.19 OutageScreen

Current

```
◆ The setup agent is temporarily unavailable.

Status: Elevated error rates affecting Anthropic API requests via gateway.
Status page: https://status.anthropic.com

The wizard may not work reliably while services are affected.

· Re-checking… (attempt 1 of 10)

 Continue anyway?

    Continue [Enter]
    Cancel [Esc]
```

Proposed

```
  ⚠  Service degradation
    Elevated error rates affecting Anthropic API requests via gateway.
    The wizard may not work reliably until this clears.

    Status page    status.anthropic.com

  ⠋ Re-checking… attempt 1 of 10

  ❯ Continue anyway
    Cancel
```

`◆` (filled diamond, brand glyph) is reserved for the brand mark in the top bar. `⚠` reads as warning. The "Status:" / "Status page:" label pair becomes a single key/value row. Picker labels lose their `[Enter]`/`[Esc]` annotations — the bottom hint bar already shows them.

---

## 5. Color spec (no change to the palette, just usage rules)

| Role | Token | Used for |
|---|---|---|
| **Brand stripe** | `Brand.lilac` → `Brand.blue` gradient | The `▎` in the top-left of the top bar. |
| **Active picker chevron** | `Brand.violet` (`Colors.active`) | `❯` in front of the highlighted picker row. |
| **In-progress** | `Brand.violet` (`Colors.active`) | `◐` task glyph, journey-stepper active dot. |
| **Completed** | `Brand.lilac` (`Colors.accentSecondary`) | `●`/`✓` task glyph, completed step in stepper. |
| **Pending** | `Colors.muted` | `○` task glyph, future step in stepper. |
| **Heading** | `Colors.heading` (Gray 10) | Bold first row of every screen. |
| **Body** | `Colors.body` (Gray 30) | Subtitle, picker labels, key/value cells. |
| **Hint / muted** | `Colors.muted` (Gray 50) | Right-column hint text, advisories, bottom hint bar. |
| **Success** | `Colors.success` | `✓` in OutroScreen (success), Slack confirmed, etc. |
| **Error** | `Colors.error` | `✗` in OutroScreen (error), validation messages. |
| **Warning** | `Colors.warning` | `⚠` heading prefix on OutageScreen + IntroScreen "no manifest" advisory. |
| **Border** | `Colors.border` | The single `─` separator under the top bar (only when present). |

---

## 6. Phasing suggestion

If we want to ship this incrementally rather than as one big PR:

1. **Chrome only.** New top bar + bottom hint bar. Every screen still works because `App.tsx` already isolates chrome from content.
2. **Picker primitive.** Replace `▸ [n]` with `❯` + right-aligned key hints. One change in `PickerMenu` flips every screen.
3. **Per-screen content rewrites.** IntroScreen → AuthScreen → RunScreen → OutroScreen (highest blast-radius first), then the rest.
4. **Glyph language.** Unify `◐`/`○`/`●` across stepper, task list, and discovery.

Step 1 gets ~60% of the perceived "new look" with ~10% of the diff — if we want to test the new chrome early, that's the cheapest change.

---

## 7. Open questions

- **Keep journey stepper labels on narrow terminals?** Currently ≥60 cols shows labels, <60 shows dots. Proposal collapses labels at <100 cols (because the breadcrumb is sharing the row). Worth confirming we don't lose accessibility.
- **`▎` brand glyph.** Renders as a thin vertical bar in most terminal fonts; some legacy fonts (Consolas in Windows Terminal pre-2021) render it as a full-width block. Need to test or fall back to `│` if unsupported.
- **Emoji removal.** `🎉` and `💬` are user-visible removals. Worth running by Marketing — the wizard's success state landing as "✓ Amplitude is live" is more sober than current; some teams prefer the celebration. Easy to keep emoji as an opt-in under `--celebrate` if desired.
