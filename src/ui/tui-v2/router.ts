/** Re-export v1 router — v2 shares the same navigation logic. */
export { WizardRouter, Screen, Flow, Overlay } from '../tui/router.js';
export type { FlowEntry, ScreenName } from '../tui/router.js';
